import AuthPage from '../en/auth/page';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Вход | Дентална Клиника',
  description: 'Влезте във вашия профил.',
};

export default AuthPage;
